Here is the regenerated, fully detailed, self‑contained research plan — a complete execution guide. Every module is described with explicit instructions for code development, computational tasks, validation checks, and the final deliverable reports. The plan assumes a standard scientific Python stack (NumPy, SymPy, mpmath, Matplotlib, SciPy, etc.) and a researcher executing each step sequentially.

---

# Adelic Physics Research Programme: Execution Plan

**Goal:** Investigate whether the adelic (simultaneous real and *p*‑adic) completion of the rational numbers forces the fine‑structure constant and other dimensionless physical quantities to specific number‑theoretic values, and extract insights about prime numbers from this richer calculus.

**Method:** Develop a computational library for *p*‑adic and adelic arithmetic, then build models (harmonic oscillators, string amplitudes, renormalisation group, spectral action) that impose adelic product constraints. Numerically solve for the values of couplings and mass ratios, compare with experimental data, and attempt to discover closed‑form expressions.

**Structure:** Ten sequentially dependent modules. Each module is a deliverable unit containing:
- Python source file(s) (`.py`)
- A Jupyter notebook or Markdown report (`.ipynb` / `.md`) with narrative, code outputs, plots, and conclusions.
- A data file if large numerical results are generated.

**Execution:** The plan is self‑contained; a researcher follows the instructions step by step. All code blocks are described algorithmically; the researcher writes and tests the code, validates outputs using the provided criteria, and assembles the final repository.

---

## Module 1: Foundational Library – p‑adic and Adelic Arithmetic

**Objective:** Implement the number‑theoretic objects that underpin the entire programme: *p*‑adic numbers, the adele ring $\mathbb{A}_{\mathbb{Q}}$, and the adelic absolute value. Verify the product formula and Ostrowski’s theorem numerically.

**Detailed instructions:**

1. **Create `padic.py`:**
   - Implement a class `PadicNumber` for a fixed prime $p$:
     - Constructor accepts a rational `q` (as `Fraction`) and optionally a pre‑computed valuation and unit part.
     - Store the prime $p$, valuation $v_p(q)$ (exponent of $p$ in factorisation), and the unit part $u = q \cdot p^{-v_p(q)}$ (rational with no factor $p$).
     - Implement:
       - `norm()`: returns $p^{-v_p(q)}$.
       - `valuation()`: returns $v_p$.
       - `unit()`: returns $u$ as a `Fraction`.
       - `__repr__`: human‑readable.
       - Arithmetic: `__add__`, `__sub__`, `__mul__`, `__truediv__` using *p*‑adic Hensel expansion. For simplicity, perform rational arithmetic and then recompute valuation/norm (since the result is again a rational, exact).
       - `__eq__` based on rational equality.
     - *Note*: For later analytic operations, we may need full Hensel expansion for series; implement a method `expansion(k)` that returns the first $k$ digits of the *p*‑adic expansion (as list of integers $0\le a_i < p$), using Hensel lifting on the rational representation.
   - Write unit tests:
     - For a sample prime (e.g., $p=2$), test addition of $1$ and $2$ to ensure the norm behaves correctly.
     - Verify $|x|_p = 0$ iff $x=0$ (but $0$ is a special case, valuation infinite — handle with a sentinel like `valuation = None` and norm `0`).
     - Test product of norms: $|xy|_p = |x|_p |y|_p$.

2. **Create `adele.py`:**
   - Implement a class `Adele`:
     - Constructor accepts a real number `r` (float or high‑precision mpmath float) and a dictionary `padic_dict` mapping prime `p` (int) to `PadicNumber` (or rational). The dictionary must have almost all entries with $|x_p|_p \le 1$ (i.e., the *p*‑adic integer condition). For simplicity, we’ll only work with rational numbers as input, so that condition is automatically satisfied for all but finitely many primes: the adele of a rational $q$ is $(q, (q \text{ as PadicNumber for each } p))$. Provide a helper `from_rational(q: Fraction)` that constructs an adele whose real component is `float(q)` and whose *p*‑adic components are `PadicNumber(p, q)` for all primes up to some large bound (or store only those primes dividing numerator/denominator, and treat missing primes as having valuation $\ge0$, norm $\le 1$ — implement lazy evaluation).
     - For general adeles (like in products of local partition functions), we’ll need to store arbitrary local values, but initially it’s enough to handle rationals.
     - Implement `norm()`: return $\prod_{v} |x_v|_v$, where $v$ runs over real (absolute value) and all stored primes (norm of PadicNumber). For missing primes, $|x_p|_p=1$.
     - Implement product formula verification: for any rational $q$, `Adele.from_rational(q).norm()` must equal $1$ exactly (or to numerical precision for real part, but note the real absolute value is $|q|$ as float, which for rationals may be represented exactly if we use `Fraction` and `float`; strict exactness requires symbolic rational norm, so use `Fraction` for real norm, then product of `Fraction` and `Fraction` from PadicNumber norm (which returns `Fraction` as $p^{-v}$) — total product should be `Fraction(1,1)`). Implement `norm_fraction()` that returns an exact `Fraction` by handling real norm as `abs(q)` as `Fraction` (since `q` rational, `abs(q)` is a rational).
   - Unit tests: pick several rationals (e.g., $2/3, 144/625, 10, 1$) and assert product formula yields exactly 1.

3. **Create demonstration notebook `01_ostrowski_demo.ipynb`:**
   - Import `padic` and `adele`.
   - For a sequence of rational numbers, display a table: rational, real absolute value, *p*‑adic norms for $p=2,3,5,7,11$, and the total adelic product.
   - Show that the product is always $1$.
   - Brief discussion of Ostrowski’s theorem: the only non‑trivial completions of $\mathbb{Q}$ are $\mathbb{R}$ and $\mathbb{Q}_p$. Provide references (e.g., Koblitz).
   - Plot (using `matplotlib`) the real norm and a few *p*‑adic norms as bar charts for a set of rationals to visually illustrate the complementarity.

**Deliverables:**
- `padic.py` with `PadicNumber` class and tests.
- `adele.py` with `Adele` class, `from_rational` helper, and exact product formula check.
- Jupyter notebook `01_ostrowski_demo.ipynb` containing the table, verification, and commentary.

---

## Module 2: p‑adic Analysis – Derivatives, Integrals, and Harmonic Oscillator

**Objective:** Port real analytic tools to $\mathbb{Q}_p$: Vladimirov’s fractional derivative, Haar integration, and the *p*‑adic harmonic oscillator. Compute its partition function numerically.

**Background:** The Vladimirov operator $D^\alpha$ acts on functions $f:\mathbb{Q}_p\to\mathbb{C}$ via
$$ (D^\alpha f)(x) = \frac{1}{\Gamma_p(-\alpha)} \int_{\mathbb{Q}_p} \frac{f(y)-f(x)}{|y-x|_p^{1+\alpha}} \, dy , $$
with $\Gamma_p$ the Morita *p*‑adic gamma. The *p*‑adic harmonic oscillator Hamiltonian is often taken as $H = D^1 + V$, with a potential like $V(\phi) = |\phi|_p^\rho$.

**Detailed instructions:**

1. **Implement Haar measure and integration (`padic_analysis.py`):**
   - Write a function `haar_measure(p)` that returns a function `mu(ball_radius)` giving the volume of the ball $\{x: |x|_p \le p^{-n}\}$ which equals $p^{-n}$; sphere $\{x: |x|_p = p^{-n}\}$ volume is $p^{-n}(1-p^{-1})$.
   - Create a numerical integration routine `integrate(f, p, radius, N_samples)` for a compactly supported function `f` on $\mathbb{Q}_p$:
     - Decompose $\mathbb{Q}_p$ into concentric spheres: for $|x|=p^{-n}$, sample the unit sphere (the set of units) uniformly? For radial functions (depending only on $|x|_p$), integration reduces to sum over spheres.
     - Since we’ll primarily deal with radial functions, implement `integrate_radial(radial_function, p, cutoff_radius)` that sums over all $n$ from $-\infty$ to $\infty$ (or up to a cutoff) the value of `radial_function` evaluated at a representative with norm $p^{-n}$ multiplied by the sphere volume.
   - Write test cases: integrate the indicator of the unit ball (should yield 1), and the norm function $|x|_p$ over the unit ball.

2. **Vladimirov derivative (symbolic/numerical):**
   - For a radial function $f(x)=g(|x|_p)$, the Vladimirov derivative $D^1 f$ can be expressed as a summation formula (see Vladimirov, Volovich, Zelenov). Look up the explicit formula for radial functions: for $|x|_p = p^{-k}$,
     $$ (D^1 f)(x) = \frac{p-1}{p^2} \sum_{n=-\infty}^\infty p^{-n} \bigl[ f(p^{-n}) - f(p^{-k}) \bigr] \frac{1}{|p^{-n}-p^{-k}|_p^2} . $$
   - Implement a function `vladimirov_D1_radial(f_radial, p, k)` that computes $D^1 f$ at a point with norm $p^{-k}$ using a truncated sum (e.g., $n$ from $-10$ to $10$).
   - Test on simple functions: if $f(x)=|x|_p$, the derivative should be known (it yields a distribution, but on radial test functions it’s manageable). At least verify that applying to the constant function $1$ gives $0$ approximately.

3. **p‑adic harmonic oscillator:**
   - Use the Hamiltonian $H = D^1 + V$ with $V(x) = |x|_p^2$ (or a similar potential). We wish to compute the partition function $Z_p(\beta) = \int \exp(-\beta H) *1$? More directly, the path‑integral formulation for the *p*‑adic oscillator yields a known spectrum: the eigenvalues are related to the *p*‑adic Hermite functions. However, a simpler route for a single‑site (zero‑dimensional) *p*‑adic φ⁴ theory is available. For the harmonic oscillator, we can consider the quantum‑mechanical energy levels. Since we lack a full spectral decomposition, we approximate $Z_p(\beta)$ by the classical partition function as a first step:
     $$ Z_p^{\text{cl}}(\beta) = \int_{\mathbb{Q}_p} e^{-\beta (|x|_p^2)} \, d\mu(x) . $$
     This integral is solvable exactly: using the Haar measure, sum over spheres $|x|=p^{-n}$:
     $$ Z_p(\beta) = 1 + \sum_{n=1}^\infty p^{-n} (1-p^{-1}) e^{-\beta p^{2n}} . $$
     The “1” comes from the unit ball where $|x|_p\le 1$ (note $|x|_p^2 = |x|_p$? Wait, $|x|_p^2$ is ambiguous: in *p*‑adic norm, $|x|_p^2 = (p^{-v})^2 = p^{-2v}$, but the potential should be a real‑valued function of the *p*‑adic norm, e.g., $V(x) = |x|_p^2$ as a real number, which is $p^{-2n}$. That’s fine.
     - Compute this series numerically for several $p$ and $\beta$, verify convergence.
   - Implement a function `padic_harmonic_oscillator_partition(p, beta)` returning $Z_p(\beta)$ using the series with sufficient terms (stop when term < 1e-15).
   - For the Archimedean (real) oscillator, the classical partition function is $Z_\infty(\beta) = \sqrt{\pi/\beta}$ for a pure $x^2$ potential, but if we want comparable normalization we should use the same quadratic potential $V(x)=x^2$ and the Lebesgue measure. Compute $Z_\infty(\beta) = \int_{-\infty}^\infty e^{-\beta x^2} dx = \sqrt{\pi/\beta}$.

4. **Deliverable notebook `02_padic_oscillator.ipynb`:**
   - Write code (import from `padic_analysis`) and compute $Z_p(\beta)$ for $p=2,3,5,7,11$ and a range of $\beta$.
   - Plot $Z_p(\beta)$ vs $\beta$ and compare with $Z_\infty(\beta)$.
   - Discuss the asymptotic behaviours and the fact that the *p*‑adic integral is bounded (as $\beta\to\infty$, $Z_p\to 1$) because the unit ball always contributes $1$, while real $Z_\infty\to0$. This is a crucial feature for adelic cancellations.
   - Verify the code by comparing the series sum for $p=2$ at a specific $\beta$ with a manual calculation.

**Deliverables:**
- `padic_analysis.py` containing integration and Vladimirov derivative functions.
- `padic_oscillator.py` containing the partition function.
- Notebook `02_padic_oscillator.ipynb` with calculations, plots, and analysis.

---

## Module 3: Adelic Harmonic Oscillator and Constrained Frequencies

**Objective:** Construct the adelic partition function $\mathcal{Z}_{\text{ad}}(\beta) = Z_\infty(\beta) \prod_{p} Z_p(\beta)$ for a single particle, and impose the “adelic product formula” constraint $\mathcal{Z}_{\text{ad}}(\beta_0) = 1$ for a fiducial temperature to see if it selects a unique dimensionless frequency (mass).

**Prerequisite:** Module 2 partition functions.

**Instructions:**

1. **Construct the adelic product:**
   - Choose a rational frequency parameter $\omega \in \mathbb{Q}_{>0}$ that sets the real potential $V_\infty(x) = \omega x^2$ and *p*‑adic potentials $V_p(x) = \omega_p |x|_p^2$ where $\omega_p = |\omega|_p$ (the *p*‑adic norm of the same rational number).
   - Real partition function becomes $Z_\infty(\beta; \omega) = \sqrt{\frac{\pi}{\beta\omega}}$ (since rescale $x$). *p*‑adic partition function depends on $\omega_p$ through the exponent: $Z_p(\beta; \omega_p) = 1 + \sum_{n=1}^\infty p^{-n}(1-p^{-1}) e^{-\beta \omega_p p^{2n}}$.
   - The adelic partition function is $\Xi(\beta, \omega) = Z_\infty(\beta,\omega) \prod_p Z_p(\beta,\omega_p)$.
   - Because $\omega$ is rational, only finitely many primes have $\omega_p \neq 1$ (i.e., primes dividing numerator or denominator). For all others, $\omega_p = 1$, and the *p*‑adic factor $Z_p(\beta,1)$ can be precomputed once.

2. **Define the constraint:**
   - Set a fiducial inverse temperature $\beta_0 = 1$ (in natural units). The adelic invariance condition: $\Xi(\beta_0, \omega) = 1$ exactly.
   - Write a function `adelic_product(beta, omega_frac)` that computes the product using a selection of primes (all primes up to some bound and including those dividing $\omega$). The infinite product over all primes of $Z_p(\beta,1)$ may need to be regularised. In fact, the product over all primes of $Z_p(\beta; 1)$ likely diverges unless we introduce a regulator. To avoid this complexity, we can consider the **ratio** $\Xi(\beta,\omega)/\Xi(\beta,1)$, or we can fix a “reference” adelic partition function that is assumed to equal 1 for a specific $\omega$. A cleaner approach: in physical applications, the adelic product formula for a string amplitude holds for the *full* adelic amplitude, not the partition function of a single oscillator. For our toy, we can work with the *difference*: we hypothesise that the partition function itself should satisfy $\Xi = 1$ when $\omega$ takes a particular value that we seek. Since the product over all primes of $Z_p(\beta,1)$ is a constant (though possibly infinite), we can regularise by truncating at a large prime $P$ and checking convergence. To make the problem well‑posed, we’ll define a truncated product up to a maximum prime $P_{\max}$ (e.g., $P_{\max}=100$) and search for $\omega$ such that $\Xi^{(P_{\max})} \approx 1$; then study the behaviour as $P_{\max}$ increases.
   - Use a root‑finding algorithm: treat $\omega$ as a rational number $a/b$ with small numerator/denominator, and evaluate $\Xi$; adjust to minimise $|\Xi-1|$.

3. **Numerical search and analysis:**
   - Write a script that enumerates fractions $\omega = n/d$ with $n,d$ small (e.g., up to 20), computes the truncated product $P_{\max}=50$, and reports those with $\Xi$ closest to 1.
   - If a unique value emerges, refine it by increasing precision.
   - Analyse sensitivity to $\beta_0$: repeat for other $\beta_0$ values; the selected $\omega$ might scale. A truly dimensionless constant should be independent of $\beta_0$, which would indicate that the condition fixes a relation between mass scale and temperature, not a pure number. That’s acceptable; the fine‑structure constant is dimensionless, so we need a model where $\omega$ is already dimensionless (e.g., mass in Planck units). So set $\beta_0$ as some fixed dimensionless number, e.g., $1$. The result will be a numerical value for $\omega$.
   - Document whether the value is near a known physical constant (e.g., electron mass $m_e/M_{\text{Pl}}$ is tiny, not $\mathcal{O}(1)$). This toy is only a stepping stone.

4. **Report `03_adelic_oscillator_constraint.ipynb`:**
   - Present the equations, the truncation method, the search results, and a discussion of the limitations.
   - Provide a plot of $\Xi$ as a function of $\omega$ over a continuous range (using real $\omega$, but evaluating *p*‑adic components using $|\omega|_p$ based on rational approximation; or treat $\omega$ as real and define its *p*‑adic norm by a chosen rational approximation).

**Deliverables:**
- `adelic_oscillator.py` containing the adelic partition function and search routines.
- Notebook `03_adelic_oscillator_constraint.ipynb` with results and analysis.

---

## Module 4: p‑adic Gamma Functions and Adelic String Amplitudes

**Objective:** Compute the Morita *p*‑adic gamma function $\Gamma_p$ and build the *p*‑adic Veneziano amplitude. Verify the adelic product formula for the four‑point amplitude and extract a condition on the string coupling, leading to a numerical estimate of the fine‑structure constant.

**Background:** The real Veneziano amplitude for open bosonic strings is
$$ \mathcal{A}_\infty(s,t) = \frac{\Gamma(-\alpha(s))\,\Gamma(-\alpha(t))}{\Gamma(-\alpha(s)-\alpha(t))}, \qquad \alpha(s) = \alpha_0 + \alpha' s. $$
The *p*‑adic amplitude is obtained by replacing $\Gamma$ with $\Gamma_p$. The adelic product formula is $\mathcal{A}_\infty(s,t) \prod_p \mathcal{A}_p(s,t) = 1$, which holds for a particular choice of the string coupling constant $g$, or forces a relation among $\alpha_0$, $\alpha'$, and the external momenta.

**Instructions:**

1. **Implement Morita’s p‑adic gamma (`padic_gamma.py`):**
   - For odd prime $p$:
     $$ \Gamma_p(z) = \lim_{n \to |z|_p^{-1}} (-1)^n \prod_{\substack{k=1 \\ p\nmid k}}^n k , $$
     but more tractably, we can use the definition for integers:
     $$ \Gamma_p(n) = (-1)^n \prod_{\substack{k=1 \\ p\nmid k}}^{n-1} k , \qquad n\ge 1, $$
     and extend using functional equations. For numeric evaluation, we can restrict to $z\in\mathbb{Z}_p$ (rational integers). Since the amplitude arguments will be integers (Mandelstam variables scaled to be integers), it suffices to implement $\Gamma_p(n)$ for integer $n$.
   - Write `padic_gamma(p, n)` that returns the value of $\Gamma_p(n)$ as a rational number (actually product of integers, so an integer). Handle $n=0$ as $-1$? Actually, $\Gamma_p(0)=1$ by convention; check functional equation.
   - Validate using known values: e.g., $\Gamma_p(n) \Gamma_p(1-n) = (-1)^{n_0}$ where $n_0$ is the integer in $\{1,\dots,p-1\}$ congruent to $n$, times $\pm1$. Provide test cases.
   - Implement extended valuation and norm-wise: because the *p*‑adic absolute value of $\Gamma_p(z)$ can be obtained using the Legendre formula for the product.

2. **Build Veneziano amplitude (`string_amplitudes.py`):**
   - Write `real_veneziano(s, t, alpha0=1, alphap=1)` returning $\mathcal{A}_\infty$ using `math.gamma` (or `mpmath.gamma` for high precision).
   - Write `padic_veneziano(p, s, t, alpha0, alphap)` that computes $\mathcal{A}_p$ using `padic_gamma`. Because $\Gamma_p$ takes integer arguments, ensure that $-\alpha(s)$, etc., are integers by a suitable choice of momenta or by taking $\alpha'$ as an integer and $\alpha_0$ integer. This is a restriction we can impose: select a Regge trajectory with integer intercept and integer slope, and integer external momenta (in appropriate units). This is common in *p*‑adic string theory where all worldsheet coordinates are *p*‑adic, and the amplitude is a rational function.
   - For a chosen set of integers (s, t, u) such that $s+t+u = -4$ (for four‑tachyon scattering? Actually for open string tachyon external, $s+t+u = -8$? We can just pick a concrete example from the literature: Freund & Witten’s paper uses $s = t = u = -2$ and $\alpha(s)=-s/2-1$? Let’s define $\alpha(s) = -s/2 - 1$, so at $s=-2$, $\alpha(-2)=0$ giving simple arguments. We’ll replicate their known result. Use the exact formulas from the literature: the p‑adic amplitude for the four‑point function of the open p‑adic string is
     $$ \mathcal{A}_p(s,t) = \frac{(1-p^{-1-\alpha(s)}) (1-p^{-1-\alpha(t)})}{(1-p^{-1-\alpha(s)-\alpha(t)})} \cdot (\text{some normalization}) . $$
     Actually, there is a known exact expression: 
     $$ \mathcal{A}_p(s,t) = \frac{\Gamma_p(-\alpha(s)) \Gamma_p(-\alpha(t))}{\Gamma_p(-\alpha(s)-\alpha(t))} . $$
     Some references use the Morita gamma. We’ll stick with that.
   - Compute adelic product `A_adelic = real_veneziano(s,t) * product_{p in primes set} padic_veneziano(p,s,t)` for a finite set of primes (e.g., up to 100). Check if it equals 1 (or a constant).

3. **Extract the coupling from adelic invariance:**
   - In the real world, the string amplitude is proportional to the string coupling $g_s$, which is related to the gauge coupling. Introduce a factor $C$ in the real amplitude such that the product formula forces $C$ to a specific value. That is, set $\mathcal{A}_\infty(s,t) = C \cdot \text{Gamma expression}$, and demand $\mathcal{A}_\infty \prod_p \mathcal{A}_p = 1$ for a specific kinematic point. Solve for $C$.
   - Then $C$ is proportional to $g_s^2$ (or the electromagnetic coupling $e^2$). Compute $C$ numerically.
   - Compare $C$ with the known fine‑structure constant (or rather, with the unified coupling at the string scale). A direct comparison is subtle, but the order of magnitude may be close to $\sim 1/100$.
   - Run the calculation for several different primes sets and kinematic points to see if $C$ is consistent. Document the value.

4. **Report `04_adelic_veneziano.ipynb`:**
   - Show the computation of $\Gamma_p$ for test values.
   - Display the real and p‑adic amplitudes for sample (s,t).
   - Show the adelic product and the determined factor $C$.
   - Discuss the discrepancy with $\alpha \approx 1/137$ and suggest corrections (e.g., supersymmetry, multiple dimensions).

**Deliverables:**
- `padic_gamma.py` with Morita gamma implementation.
- `string_amplitudes.py` with real and p‑adic Veneziano functions.
- Notebook `04_adelic_veneziano.ipynb` with calculations and discussion.

---

## Module 5: Hierarchical Renormalisation Group and Adelic Fixed Points

**Objective:** Demonstrate that the discrete renormalisation group (RG) on a *p*‑adic hierarchical lattice (Bethe lattice) yields recursion relations whose fixed points, when combined with Archimedean RG and the adelic product constraint, determine the critical exponents to number‑theoretic values.

**Background:** The Dyson hierarchical model has recursion for the coupling $\lambda$:
$$ \lambda_{n+1} = f(\lambda_n) = \frac{ \int d\mu(x)\, e^{-x^2/2 - \lambda_n x^4} \, \text{(some kernel)} } $$
A simpler version exists for the *p*‑adic $\phi^4$ theory on the Bruhat–Tits tree: after one decimation step, the new coupling $\lambda'$ is given by an explicit rational function.

**Instructions:**

1. **Derive the *p*‑adic recursion map:**
   - Consult the literature (e.g., “Renormalization group on hierarchical lattices”, or the exact solution of the *p*‑adic Ising model). For a scalar field with action $S = \frac12 \sum_{\text{edges}} (\phi_i - \phi_j)^2 + \sum_i V(\phi_i)$ on a *p*-adic tree (Bethe lattice of coordination $p+1$), the recursion relation for the “effective” local potential $V$ can be approximated. For a $\phi^4$ theory, the map for the quartic coupling $\lambda$ is:
     $$ \lambda \to \lambda' = \frac{ g_4(\lambda) }{ g_2(\lambda)^2 } \cdot \text{(factor)} , $$
     where $g_2$, $g_4$ are integrals of the Boltzmann weight.
   - To keep the module self‑contained, we can use a simplified map that captures the discrete RG step: 
     $$ \lambda' = \frac{ 3p \, \lambda^2 }{ (1 + \lambda)^2 } $$
     This is not arbitrary; it shares the correct fixed point structure. (Actually, the exact map for the Ising model on a *p*-adic tree is $\lambda' = \frac{p \lambda^2}{1+\lambda^2}$ or similar. We’ll research and implement an authentic map, even if approximate, by performing the necessary integrals over the *p*‑adic sphere.)
   - Implement a function `padic_rg_step(p, lambda_)` that returns the new coupling. Provide reference for the formula used.

2. **Real RG beta function:**
   - For the same $\phi^4$ theory in real Euclidean space, the one‑loop beta function (dimensionless coupling in $d=4$) is $\beta(\lambda) = 3\lambda^2/(4\pi)^2$. The continuous RG flow gives a trajectory $\lambda(\mu)$. To connect with discrete *p*‑adic steps, we can discretise the real RG by taking scale factors $b$ (e.g., $b=2$) and define a recursion $\lambda_{n+1} = R_\infty(\lambda_n)$ by integrating the beta function from $\mu$ to $\mu/b$. Since we don’t yet have a unified scale, we’ll work with the fixed‑point value.

3. **Adelic constraint:**
   - Consider the adelic partition function (free energy) of the hierarchical model. The product of partition functions over all places should be scale‑invariant. That translates to a condition linking the coupling at the real fixed point and the *p*‑adic fixed points.
   - More concretely, we can impose that the product over primes of the *p*‑adic fixed‑point coupling (or a function thereof) equals the real fixed‑point coupling. For example, if $\lambda_*^{(\infty)}$ is the real IR fixed point, and $\lambda_*^{(p)}$ are the *p*‑adic fixed points of the hierarchical recursion, the adelic condition could be $\lambda_*^{(\infty)} = \prod_p \lambda_*^{(p)}$ (or something similar). Determine the appropriate relation by examining the product formula for the effective action.
   - Numerically compute the *p*‑adic fixed points for several $p$ by iterating the map to convergence.
   - Compute the real fixed point from the continuous beta function (for $d=4$, it’s at $\lambda=0$ trivial; for $d<4$, we need a non‑trivial fixed point. We can work in fractional dimension or use the hierarchical RG for real space). A simpler approach: use the hierarchical lattice also for the real case with coordination number very large to approximate the real continuous limit, or just take the 2D Ising critical point value known analytically ($\lambda_*$). The goal is to exhibit that the adelic product of local fixed point values yields a striking number (like $1/2$ or a zeta value) that corresponds to a physical critical exponent.

4. **Compute critical exponent:**
   - The correlation length critical exponent $\nu$ is given by $\nu = \log b / \log \lambda_{\text{eigen}}$, where $\lambda_{\text{eigen}}$ is the largest eigenvalue of the linearised RG map at the fixed point.
   - For the *p*‑adic map, compute the eigenvalue analytically (derivative of $f$ at $\lambda_*$), and then $\nu_p$. Show that the adelic condition relates these to a number‑theoretic value, e.g., $\nu = \log 2 / \log(2\cos(\pi/8))$ for the Ising model. Check if the adelic product condition forces $\nu$ to that exact expression.

5. **Notebook `05_hierarchical_RG_adelic.ipynb`:**
   - Document the exact recursion formula for a chosen model.
   - Plot the *p*‑adic RG flow (cobweb).
   - Compute fixed points and report their values.
   - Compute the adelic combination and compare with real critical exponent.
   - Conclude with a discussion of how this mechanism could fix α.

**Deliverables:**
- `hierarchical_rg.py` containing recursion maps.
- Notebook `05_hierarchical_RG_adelic.ipynb` with analysis.

---

## Module 6: QED Beta Function from Adelic Sum Rules

**Objective:** Derive the one‑loop beta‑function coefficient of QED from an adelic product formula over prime‑charged loops, and use the high‑scale adelic constraint to predict the low‑energy fine‑structure constant $\alpha(M_Z)$.

**Instructions:**

1. **Adelic vacuum polarisation model:**
   - Represent the photon self‑energy as a sum over prime topologies: each prime $p$ contributes a *p*‑adic string loop integral. The real vacuum polarisation is the real part of the adelic amplitude.
   - Use the adelic string amplitude from Module 4 with an external photon insertion. In the effective field theory limit, the photon self‑energy $\Pi(q^2)$ gets contributions that can be expressed as a sum over primes of some function.
   - Alternatively, take a phenomenological model: assume the running of $\alpha$ is given by a discrete product formula reminiscent of the Euler product for the zeta function. For example, conjecture that
     $$ \alpha(\mu) = \alpha_0 \prod_{p\le \mu/M_0} f_p(\alpha) , $$
     where $f_p$ are rational functions derived from *p*‑adic amplitudes.
   - We can attempt to match the standard one‑loop QED beta function $b_0 = \frac{1}{3\pi} \sum_f Q_f^2$ by expanding the product formula in $\alpha$ and comparing coefficients. This will link the adelic parameters to the charges.

2. **Derive an adelic RG equation:**
   - Write the adelic product invariance for the photon propagator at two different renormalisation scales $\mu_1$, $\mu_2$:
     $$ \frac{D_{\infty}(\mu_1)}{D_{\infty}(\mu_2)} \prod_p \frac{D_p(\mu_1)}{D_p(\mu_2)} = 1 . $$
   - In the *p*‑adic sector, the scale change corresponds to multiplying the *p*‑adic norm by $p^{-1}$, which is a discrete jump. So the ratio $D_p(\mu_1)/D_p(\mu_2)$ is $p$ raised to some power involving $\alpha_p$.
   - Use the explicit tree‑level *p*‑adic propagator from string theory (or a toy QED on *p*‑adic space) to get these ratios.
   - Linearise to obtain a discrete beta function, then take the Archimedean limit (large $p$, continuous scaling) to recover the continuous beta function. This will show that $b_0$ is determined by the sum over primes $\sum_p \frac{1}{p^s}$ for some $s$.

3. **Numerical prediction for $\alpha(M_Z)$:**
   - Use the adelic constraint fixed at the Planck scale (or the string scale) to provide the initial condition for the running. For example, assume that at the Planck scale the adelic product condition yields $\alpha(M_{\text{Pl}}) = 1/137.036$ or some derived value. Then run the continuous beta function down to the Z‑boson mass scale $M_Z\sim 91$ GeV using the known SM beta function (including weak and strong corrections if needed) and see if it agrees with the measured $\alpha(M_Z) \approx 1/127.9$.
   - Alternatively, solve the adelic constraint directly for $\alpha(M_Z)$ without free parameters by requiring the full adelic product (including all scales) to be exactly 1.
   - The computation will involve integrating the beta function numerically using SciPy, integrating from $M_{\text{Pl}}$ to $M_Z$.

4. **Notebook `06_adelic_QED_beta.ipynb`:**
   - Present the derivation of the adelic beta function.
   - Show the numerical integration and the predicted $\alpha(M_Z)$.
   - Compare with the experimental value, produce a discrepancy plot.
   - Discuss missing thresholds, supersymmetry, etc.

**Deliverables:**
- `qed_beta.py` containing adelic beta function and running routines.
- Notebook `06_adelic_QED_beta.ipynb` with full analysis.

---

## Module 7: The Riemann Zeta as an Adelic Partition Function — Mass Ratios

**Objective:** Interpret the completed Riemann zeta function $\hat{\zeta}(s)$ as the partition function of an adelic conformal field theory (a “prime gas”) and explore whether the statistics of its non‑trivial zeros encode particle mass ratios.

**Instructions:**

1. **Adelic partition function representation:**
   - Write a function `completed_zeta(s)` that computes $\pi^{-s/2}\Gamma(s/2)\zeta(s)$ using `mpmath`.
   - Demonstrate that the local factor at each prime is $\zeta_p(s) = (1-p^{-s})^{-1}$, and the real factor is $\Gamma_\mathbb{R}(s) = \pi^{-s/2}\Gamma(s/2)$.
   - Show that the functional equation $\hat{\zeta}(s) = \hat{\zeta}(1-s)$ is the adelic modular invariance.

2. **Zero statistics:**
   - Use a high‑precision library (mpmath) to compute the first $N$ non‑trivial zeros (say $N=1000$) on the critical line.
   - Compute the nearest‑neighbor spacings $\delta_n = \gamma_{n+1}-\gamma_n$, normalized to unit mean. Plot the histogram and compare with the GUE (Gaussian Unitary Ensemble) Wigner surmise $P(s) = \frac{32}{\pi^2} s^2 e^{-4s^2/\pi}$.
   - Perform a Kolmogorov–Smirnov test to quantify the agreement.

3. **Mass ratio extraction:**
   - Assume that particle masses are proportional to zero spacings, i.e., $m_k \propto \delta_k$, sorted by value. Assign the first few masses to known fermions (e.g., electron, muon, tau, and quarks) in order.
   - Compute the dimensionless ratios $m_k/m_1$ and compare with experimental ratios: $m_e/m_e=1$, $m_\mu/m_e \approx 206.768$, $m_\tau/m_e \approx 3477$, $m_u/m_e\approx 4.8$ (up quark is tricky because of confinement; use MS-bar masses at high scale or current quark masses), etc.
   - Use a least‑squares fit to scale the proportional constant to minimize the deviation. Evaluate the goodness of fit.
   - Alternatively, look for patterns directly in the zeros: the difference between consecutive zeros might follow certain harmonic sequences. If some sequence corresponds to a known ratio, report it.

4. **Search for $\alpha$ in zeta zeros:**
   - Encode the fine‑structure constant: compute $1/\alpha \approx 137.036$. Check if any simple function of the zeros (e.g., sum of reciprocals, product of first few zeros) equals this value. For example, check if $\prod_{n=1}^{100} (1 - 1/\gamma_n)$ approximates $1/137$. Perform a systematic search using symbolic regression of simple expressions involving $\pi$, $e$, $\gamma$ (Euler constant), $\zeta(3)$, etc., on the zero sequence.

5. **Report `07_zeta_mass_ratios.ipynb`:**
   - Plots of zero spacings and GUE overlay.
   - Table of predicted vs. experimental mass ratios.
   - Discussion on the speculative nature and possible interpretation as masses of a “spectral particle”.
   - The search for $\alpha$ and any candidate expression.

**Deliverables:**
- `zeta_analysis.py` with zero computation and spacing analysis.
- Notebook `07_zeta_mass_ratios.ipynb` with results and interpretation.

---

## Module 8: Cross‑Ratio Projective Geometry and $\alpha$ as a Cross‑Ratio

**Objective:** Recast the electromagnetic coupling as a cross‑ratio in an adelic projective space and compute its value from pure geometry, inspired by Wyler’s formula.

**Instructions:**

1. **Wyler’s approach (review):**
   - Provide a short mathematical background: In the conformal group $SU(2,2)$, certain symmetric spaces (e.g., $SU(4)/S(U(2)\times U(2))$) have volumes that can be expressed in terms of $\pi$ and other constants. Wyler expressed $\alpha$ as a ratio of such volumes.
   - Modern formulation: The on‑shell photon propagator can be represented as a period integral of a motive, and $\alpha$ is the value of a cross‑ratio of four points on the boundary of a four‑dimensional hyperbolic space.

2. **Adelic cross‑ratio model:**
   - Define the cross‑ratio $[z_1,z_2;z_3,z_4] = \frac{(z_1-z_3)(z_2-z_4)}{(z_1-z_4)(z_2-z_3)}$.
   - In adelic terms, the four points are rational numbers (or more generally adelic points). The cross‑ratio $C$ is a rational number. The adelic product formula for amplitudes says that the physical coupling $g^2 \propto \prod_v |C|_v^{\epsilon}$, where $\epsilon$ is some characteristic exponent.
   - Conjecture: The fine‑structure constant is given by $\alpha = (\text{constant}) \cdot \prod_v |C_v|_v^{-1}$, with $C_v$ the local cross‑ratio of special points (e.g., the fixed points of the adelic modular group).
   - Derive an equation by identifying the cross‑ratio with the kinematic invariants in the Veneziano amplitude from Module 4. For four‑tachyon scattering at a symmetric point, the cross‑ratio is $-1$, and the coupling appears as the coefficient of a logarithm. Combine with adelic product to solve for $\alpha$.

3. **Numerical computation:**
   - Use the known value of the cross‑ratio from string theory ($z = -1$) and compute the adelic product $|C|_\infty \prod_p |C|_p$ for $C$ taken as a rational number modeling the scattering geometry. The norm product is 1, so it doesn’t directly give a non‑trivial number. Instead, look at the *logarithmic* derivative: the coupling is the residue of the pole in the cross‑ratio channel, which involves derivatives of the gamma functions. The adelic product of the residues might yield $\alpha$.
   - Implement a computation that numerically evaluates the derivative of the adelic amplitude with respect to the cross‑ratio at the symmetric point, and equate it to $1/\alpha$.
   - This will involve the previous modules’ amplitude code.

4. **Report `08_cross_ratio_alpha.ipynb`:**
   - Mathematical derivation.
   - Numerical execution using the adelic string amplitude.
   - Resulting value for $\alpha$ and comparison with data.

**Deliverables:**
- Notebook `08_cross_ratio_alpha.ipynb` with full computation and commentary.

---

## Module 9: Cosmic Galois Group, Periods, and Symbolic Regression for $\alpha$

**Objective:** Use the mathematical framework of motivic periods (the “cosmic Galois group”) to express $\alpha$ as a period, and employ symbolic regression combined with adelic constraints to discover a closed‑form formula.

**Instructions:**

1. **Generate a database of adelic special values:**
   - Compute a set of candidate numbers: $\pi$, $e$, $\Gamma(1/4)$, $\Gamma(1/3)$, $\zeta(2), \zeta(3), \dots$, multiple zeta values, and *p*‑adic gamma values at special arguments.
   - Also include arithmetic expressions involving prime numbers, e.g., $\prod_p (1-p^{-2})^{-1} = \zeta(2) = \pi^2/6$, etc.
   - Store them in a Python dictionary with high precision.

2. **Symbolic regression setup:**
   - Use a library like `gplearn` (genetic programming) or a custom LLM‑guided search that combines the constants with arithmetic operations and simple functions (exp, log, sqrt) to match the target $137.035999084$.
   - Alternatively, use `SymPy` to brute‑force search over a tree of expressions up to a certain depth, evaluating the adelic constraint as a fitness: the ideal expression should also satisfy that it comes from an adelic product (i.e., it can be written as $(\prod_v f_v)$). Incorporate that as a regulariser.
   - Because of the huge search space, implement heuristics: restrict to expressions that are periods of mixed Tate motives known to appear in quantum field theory (e.g., multiple zeta values, gamma values).
   - Perform the search and record the top candidates.

3. **Validation:**
   - For the best candidate expression, use the adelic string amplitude code to see if it can be derived from the product formula by an appropriate choice of the string vacuum. This is a manual analytic check.
   - If possible, derive the expression from known mathematics (e.g., volumes of hyperbolic simplices, or from Connes–Marcolli’s cosmic Galois group action).

4. **Report `09_symbolic_regression_alpha.ipynb`:**
   - Description of the search methodology.
   - List of top candidate formulas with their precision.
   - The chosen “best” formula and its physical plausibility.

**Deliverables:**
- `period_database.py` with constants.
- Notebook `09_symbolic_regression_alpha.ipynb` with search code and results.

---

## Module 10: Synthesis and Roadmap for a Complete Adelic Theory

**Objective:** Assemble all findings, evaluate their implications, and produce a final report that maps the computational evidence to a proposed adelic Standard Model, along with a GitHub repository.

**Instructions:**

1. **Compile results from Modules 1–9:**
   - Extract key numerical outcomes: the fine‑structure constant candidate values, selected frequencies, critical exponents, mass ratios.
   - Create a summary table comparing predictions with experimental data.

2. **Identify theoretical gaps:**
   - For each model, list assumptions and where more rigorous mathematics or physics is needed (e.g., fermion doubling, supersymmetry, adelic gauge symmetry breaking).
   - Discuss why exact agreement wasn’t achieved and what steps could remedy that (e.g., including superpartners, using non‑trivial adelic ground state, going beyond tree level).

3. **Propose a Lagrangian for the adelic Standard Model:**
   - Draft a schematic Lagrangian on the adele ring, coupling real and *p*‑adic fields, with adelic symmetry that is spontaneously broken to the Archimedean world.
   - This is a conceptual sketch, informed by the computational modules.

4. **Build final repository:**
   - Structure a GitHub repository `adelic-physics` with the following layout:
     - `src/` containing all Python modules (padic.py, adele.py, padic_analysis.py, etc.)
     - `notebooks/` with the numbered Jupyter notebooks and rendered PDF reports.
     - `data/` with any generated numerical data files.
     - `README.md` explaining the project, Ostrowski’s theorem, the adelic approach, and how to run the code.
     - `environment.yml` or `requirements.txt` for reproducibility.
   - Write a final paper/report `synthesis.md` (or a PDF) that summarises the programme, its findings, and a roadmap for future work.

5. **Deliverable: `10_synthesis.ipynb` and repository:**
   - The notebook itself will include the summary, gap analysis, and the roadmap.
   - The repository will be ready to upload to GitHub.

**Deliverables:**
- Notebook `10_synthesis.ipynb` containing the comprehensive synthesis.
- Complete GitHub repository ready for sharing.

---

**Execution note:** Each module should be run sequentially, using the outputs of earlier modules. All code must be documented with docstrings and tested. The researcher will commit progress to the repository after each module completion. The final repository will be a significant open‑source contribution to adelic physics.